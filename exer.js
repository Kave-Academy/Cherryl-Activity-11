function run(){

    // Declare an object
   const student = { name : "David Rayy", sclass : "VI", rollno : 12 };
  
            // Using Object.keys() method to get length
            document.getElementById("output").innerHTML = Object.keys(student).length;

}